spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aad
    username: postgres
    password: postgres
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
    show-sql: true
    properties:
      hibernate:
        format_sql: true