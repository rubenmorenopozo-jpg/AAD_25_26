server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aad_db
    username: user
    password: pass
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true